# Mantine UI library overlay

This overlay wires a Mantine provider into the generated app.

Files added:

- src/providers/mantine-provider.tsx

Dependencies added when selected: `@mantine/core`, `@mantine/hooks`.
