# Headless UI overlay

Provides Headless UI primitives and Heroicons for Tailwind-based component
creation. When selected, `@headlessui/react` and `@heroicons/react` are added
as dependencies and example primitives are provided.

Files added:

- docs on usage

Notes:

Headless UI pairs well with shadcn, DaisyUI, or custom Tailwind setups.
