{
  "name": "@darkalpha/nova",
  "version": "0.1.9",
  "description": "Nova is an extensible Next.js development toolkit for creating, generating, managing, and maintaining production-ready applications.",
  "type": "module",
  "bin": {
    "nova": "bin/nova.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "files": [
    "bin",
    "dist",
    "templates"
  ],
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build:core": "tsc -p packages/core/tsconfig.json",
    "typecheck:core": "tsc --noEmit -p packages/core/tsconfig.json",
    "build": "tsc -p packages/core/tsconfig.json && tsup",
    "dev": "tsx ./src/index.ts",
    "prepack": "tsc -p packages/core/tsconfig.json && tsup",
    "prepublishOnly": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit && tsx scripts/verify-package-manifest-sync.ts && bash scripts/verify-versions-sync.sh && tsc -p packages/core/tsconfig.json && tsup",
    "start": "node ./bin/nova.js",
    "typecheck": "tsc --noEmit -p packages/core/tsconfig.json && tsc --noEmit",
    "test": "tsx scripts/unit-test.ts && node scripts/integration-test.mjs && node scripts/smoke-test.mjs",
    "test:unit": "tsx scripts/unit-test.ts",
    "test:integration": "node scripts/integration-test.mjs",
    "test:smoke": "node scripts/smoke-test.mjs",
    "verify:manifest-sync": "tsx scripts/verify-package-manifest-sync.ts",
    "verify:versions-sync": "bash scripts/verify-versions-sync.sh",
    "verify:not-published": "bash scripts/verify-not-published.sh"
  },
  "keywords": [
    "nextjs",
    "next.js",
    "react",
    "typescript",
    "cli",
    "scaffold",
    "generator",
    "codegen",
    "developer-tools",
    "framework",
    "starter",
    "template"
  ],
  "dependencies": {
    "@clack/prompts": "^0.8.2",
    "execa": "^9.5.1",
    "fs-extra": "^11.2.0",
    "picocolors": "^1.1.1",
    "semver": "^7.8.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^22.10.2",
    "@types/semver": "^7.8.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "license": "MIT",
  "author": "",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/darka1pha/nova.git"
  },
  "homepage": "https://github.com/darka1pha/nova#readme",
  "bugs": {
    "url": "https://github.com/darka1pha/nova/issues"
  },
  "publishConfig": {
    "access": "public"
  }
}
